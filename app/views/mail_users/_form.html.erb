<%= form_for(@mail_user) do |f| %>
  <% if @mail_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mail_user.errors.count, "error") %> prohibited this mail_user from being saved:</h2>

      <ul>
      <% @mail_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :login %><br>
    <%= f.text_field :login %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
